#include "lib.h"

void quicksort(int array[], uint length, int item) {}

int main(void) {

  int items[] = {9, 5, 8, 3, 1, 0, 6, 8, 5, 9};
  uint length = sizeof(items) / sizeof(items[0]);

  return EXIT_SUCCESS;
}
